# 主题截图

请将最终截图保存为：

- `screenshots/light.png`
- `screenshots/dark.png`

截图前安装 `Noto Serif SC` 和 `Maple Mono CN`，在 Typora 中打开根目录的
`showcase.md`，使用较宽的编辑窗口并显示左侧文件树或大纲。两个主题应截取
相同内容区域，避免包含个人路径、其他应用窗口或无关通知。

图片就位后，删除根目录 `README.md`“效果展示”部分对应图片外层的 HTML
注释标记。
